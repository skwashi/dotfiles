#!/bin/sh
[ $# -ge 1 -a -f "$1" ] && input="$1" || input="-"
if [ -f "$input" ]; then
    emacsclient -nw "$input"
else
    tempfile=$(mktemp emacs-stdin-$USER.XXXXXXX --tmpdir)
    cat $input > $tempfile
    emacsclient -alternate-editor="" -nc $tempfile
fi
